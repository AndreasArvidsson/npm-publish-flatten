{
    "name": "test",
    "version": "1.0.0",
    "main": "dist/index.js",
    "files": [
        "dist/*"
    ],
    "scripts": {
        "testOrg": "npm publish --dry-run",
        "testNoParams": "node ../ --dry-run",
        "test": "node ../ --flatten dist --flatten dist/sub --strip scripts --strip devDependencies --dry-run"
    },
    "devDependencies": {}
}